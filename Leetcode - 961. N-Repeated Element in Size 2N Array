class Solution {
public:
    int repeatedNTimes(vector<int>& nums) {
        unordered_map<int,int>mpp;
        for(auto it:nums){
            mpp[it]++;
        }
        int ans=0;
        for(auto it=mpp.begin();it!=mpp.end();it++){
            if(it->second != 1){
                ans = it->first;
            }
        }
        return ans;
    }
};
